module.exports = {
    assets: ['./src/assets/fonts']
};

// run npx react-native-asset to link
// you will get message in console for Linking ttf
// re run the app by closing it 
// if not working then npm run android